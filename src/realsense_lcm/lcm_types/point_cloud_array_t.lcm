package rs_lcm;

// Here we have a structure for sending over a set of point clouds
struct point_cloud_array_t {
    // Number of point clouds we're sending
    int32_t num_point_clouds;

    // point clouds 
    point_cloud_t point_clouds[num_point_clouds];
}
